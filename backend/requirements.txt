# ── Core API runtime ──────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9

# ── Database ───────────────────────────────────────────────────────────────────
sqlmodel>=0.0.16
pymysql>=1.1.0           # MySQL/MariaDB driver (SQLite built-in, no extra driver needed)

# ── Security / Encryption ─────────────────────────────────────────────────────
cryptography>=42.0.0

# ── Templating ────────────────────────────────────────────────────────────────
jinja2>=3.1.3

# ── Validation ────────────────────────────────────────────────────────────────
pydantic>=2.6.0
email-validator>=2.1.0

# ── AI / Gemini ───────────────────────────────────────────────────────────────
google-genai>=2.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ── Optional: OCR / PDF (install manually if you need PDF attachment parsing) ─
# These packages do NOT have Python 3.14 wheels yet. The app works without them
# (OCR imports are all lazy/guarded inside pdf_extractor.py functions).
#
# To enable PDF parsing, install in a Python 3.11 venv:
#   pip install pytesseract pillow PyMuPDF rapidocr-onnxruntime
#
pillow>=10.0.0
PyMuPDF>=1.24.0

# ── PostgreSQL (required for Supabase/Render Postgres production) ──────────────
psycopg2-binary>=2.9.9

# ── 2FA / TOTP ────────────────────────────────────────────────────────────────
pyotp>=2.9.0             # RFC 6238 TOTP one-time password generation & validation
segno>=1.6.1             # QR code generation (pure-Python, no C deps)

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler>=3.10.0      # In-process cron scheduler for SLA checker & weekly digest

# ── Browser / Device Detection ────────────────────────────────────────────────
ua-parser>=0.18.0        # Parse User-Agent strings to detect browser & OS
